{
  "name": "worker",
  "version": "1.0.0",
  "description": "The worker app that will pull submission from queue and execute them.",
  "main": "worker.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:worker": "nodemon worker.js",
    "start:server": "nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.10.3",
    "nodemon": "^2.0.22",
    "socket.io": "^4.6.1"
  }
}